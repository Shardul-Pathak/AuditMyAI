'use client'

import type { PublicAuditReportDto } from '@/utils/dto/types'

type Props = { report: PublicAuditReportDto }

/**
 * Placeholder for a future PDF-style viewer. The live public report UI is
 * implemented in `ReportPageClient`.
 */
export default function PdfViewer(_props: Props) {
  return null
}
